# Code — Chapter 06 (Planning)

Create a per-chapter virtualenv and install dependencies:

```bash
cd code/chapter_06
../setup_venv.sh
```

Place runnable examples in this folder. Keep heavy dependencies minimal until
you need provider-specific integrations.
