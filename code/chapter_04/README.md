# Code — Chapter 04 (Reflection)

Create a per-chapter venv and install dependencies:

```bash
cd code/chapter_04
../setup_venv.sh
```

Run examples from this folder. See `requirements.txt` for packages.
