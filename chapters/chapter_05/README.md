# Chapter 05 — Tool Use

See `tool_use.md` for chapter notes. Code examples are under `code/chapter_05`. Use the shared `code/setup_venv.sh` script to create a per-chapter virtual environment and install dependencies listed in `code/chapter_05/requirements.txt`.
